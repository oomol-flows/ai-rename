version: 0.0.13
scripts:
  # Script executed when the container is first loaded
  # eg: bootstrap: pip install pandas
  bootstrap: |
    npm install
    pip install -r requirements.txt
icon: https://package-assets.oomol.com/packages/ai-rename/0.0.1/files/package/icon.png
name: ai-rename
description: "%ai-rename-is-an-intelligent-oomol-workflow-that-automatically-re%"
repository: https://github.com/oomol-flows/ai-rename
displayName: "%ai-rename%"
releaseNotes: add layer
dependencies:
  oomol-file: 0.0.9
